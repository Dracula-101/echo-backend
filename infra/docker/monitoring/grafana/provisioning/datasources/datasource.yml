apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy           # Grafana backend fetches from Prometheus (not the browser)
    url: http://prometheus:9090
    isDefault: true
    editable: false         # Prevent accidental edits in the UI
